@model IEnumerable<BonTemps.Areas.Manager.Models.ContactInfo>

@{
    ViewData["Title"] = "Index";
}


<div class="row">
    <a class="col-md-5 edit-btn" asp-action="Informatie">
        <div class="">
            <i class="fas fa-address-book"></i>
            <p class="text-btn-contact">Restaurant informatie aanpassen</p>
        </div>
    </a>
    <a class="col-md-5 edit-btn" asp-action="OpeningsTijden">
        <div class="">
            <i class="far fa-clock"></i>
            <p class="text-btn-tijden">Openingstijden aanpassen</p>
        </div>
    </a>
</div>
<div class="row">
    <a class="col-md-5 edit-btn" asp-controller="Email" asp-action="index" asp-area="Systeem">
        <div class="">
            <i class="fas
               fa-envelope-open"></i>
            <p class="text-btn-email">Mail overzicht</p>
        </div>
    </a>
    <a class="col-md-5 edit-btn" asp-action="Shutdown" asp-controller="ManagerBoard" asp-area="Manager">
        <div class="">
            <i class="fas fa-power-off"></i>
            <p class="text-btn-email">Systeem Afsluiten</p>
        </div>
    </a>
</div>






@*<h1>Openingstijden</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Postcode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefoonnummer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                Open en sluitingstijden
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Adres)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Postcode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefoonnummer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>

                <td>
                    <a asp-action="Edit" class="btn btn-danger" asp-route-id="@item.Id">Aanpassen</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@
